#!/usr/bin/env node
// docx-doctor CLI — stub.
//
// Planned:
//   docx-doctor scan <file> [--ci]
//   docx-doctor fix <file> -o <out> [--rules a,b,c]
//
// See ROADMAP.md Weekend 5 — build this after scan()/fix() actually work.

console.error("docx-doctor CLI is not implemented yet. See ROADMAP.md (Weekend 5).");
process.exit(1);
